window.PartFitUI={top:(title,sub='')=>`<header class="top">${location.hash&&!location.hash.includes('home')?'<button class="back" data-back>‹</button>':'<div class="logo">PF</div>'}<div class="title">${title}${sub?`<small>${sub}</small>`:''}</div><div class="grow"></div><button class="cartBtn" data-go="order">🛒</button></header>`,nav:active=>`<nav class="bottom">${[['home','⌂','Home'],['vehicle','🚗','My Car'],['catalogue','▦','Parts'],['order','🧾','Order']].map(x=>`<button class="${active===x[0]?'on':''}" data-go="${x[0]}"><span>${x[1]}</span>${x[2]}</button>`).join('')}<button data-help><span>◉</span>WhatsApp</button></nav>`,install:()=>'<button class="installBtn" data-show-install>＋ Install app</button>',card:(p,fit,vehicle,money)=>`<article class="card product" data-product="${p.id}"><div class="pic">${p.ico}</div><div><h3>${p.name}</h3><div class="muted">${fit===true?`✓ Fits ${vehicle.make} ${vehicle.model}`:fit===false?'Check fitment':p.cat}</div><div class="price">${money(p.price)}</div><div class="stock">● In stock</div></div><button class="plus" data-add="${p.id}">+</button></article>`};
